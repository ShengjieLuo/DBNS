package com.registra

import com.registra.element
import com.registra.atomTable
import com.registra.serviceTable
import com.registra.usageTable
import com.topo.query

class serviceTable{
  val tableDictionay = Vector.empty
  val tableUsage = Vector.empty

  def insert_into_table(E:element):Boolean{
    //1. Check whether the element suits grammar standard
    //2. If true, insert it into the table
  }


